{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Single athlete's performance in a single field event",
    "description": "A single athlete's performance in a single field event, which may comprise a single- or multiple- attempts.",
    "type": "object",
    "anyOf": [
        {
            "$ref": "single_field_performance.json#"
        },
        {
            "$ref": "multiple_field_performance.json#"
        }
    ]
}
