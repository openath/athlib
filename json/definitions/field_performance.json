{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Single athlete's performance in a single field event",
    "description": "A single athlete's performance in a single field event, which may be a jump or a throw.",
    "type": "object",
    "anyOf": [
        {
            "$ref": "jump_performance.json#"
        },
        {
            "$ref": "throw_performance.json#"
        }
    ]
}
