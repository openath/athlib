import { 
    hello, 
    normalizeGender, 
    perfToFloat, 
    isFieldEvent,
    betterPerformance,
} from "./utils";

import { calcUkaAgeGroup } from "./uka_agegroups";

module.exports = {
  hello,
  normalizeGender,
  perfToFloat,
  calcUkaAgeGroup,
  isFieldEvent,
  betterPerformance,
}
